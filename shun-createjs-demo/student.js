// ==========================================
// ★ 生徒チャレンジ用ファイル (student.js) ★
// ゲーム本体を壊さずに、小さな機能を足す練習をする場所です。
// まずは下の例のコメントを外して、動きを確かめてみましょう。
// ==========================================

// 星を取ったとき、tick.js から呼ばれる関数
function studentOnCatchStar(item, gainedScore) {
  // 例1：100点以上になったら、メッセージを表示する
  // if (score >= 100) {
  //   addFloatingText(STAGE_W / 2, 250, "100点突破！", "#7dffb2", 26);
  // }

  // 例2：星を取ったときにプレイヤーを少し大きくする
  // player.scaleX = 1.15;
  // player.scaleY = 1.15;
}

// 星を逃したとき、tick.js から呼ばれる関数
function studentOnMissStar() {
  // 例：ここに自分だけの「取り逃しペナルティ」を追加してみよう
  // score = Math.max(0, score - 5);
}
